# Command Line Usage

Run all projects:

python -m src.cli

Filter by status:

python -m src.cli --status Watching

Filter by category:

python -m src.cli --category "Layer 2"

Search projects:

python -m src.cli --search Startale
