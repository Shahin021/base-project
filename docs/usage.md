# Usage

Run the tracker from the project root:

python -m src.main

The app loads project data from:

data/projects.json

You can update the JSON file to add more projects, categories, websites, and notes.
